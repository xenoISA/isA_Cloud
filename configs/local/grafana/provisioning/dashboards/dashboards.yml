apiVersion: 1

providers:
  - name: 'isA Platform'
    orgId: 1
    folder: 'isA'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
