# isA_common Dependencies - Pinned for Production Stability
#
# PINNING STRATEGY:
# - requirements.txt uses exact versions (==) for reproducible builds
# - pyproject.toml uses minimum versions (>=) since this is a library
#   that should be compatible with consumers' dependency versions
#
# To update dependencies, test compatibility first then update both files.
# Last updated: 2026-05-22

grpcio==1.62.1
grpcio-tools==1.62.1
pydantic==2.6.4
tenacity==8.2.3
python-consul2==0.1.5

# Native async database/service drivers
asyncpg==0.29.0
neo4j==5.18.0
# aioboto3 chain bumped to clear 6 urllib3 CVEs: botocore 1.34.34 capped
# urllib3<2.1; botocore 1.34.106 (via aioboto3 13.0.0 -> aiobotocore 2.13.0)
# lifts that cap, allowing urllib3>=2.7.0.
aioboto3==13.0.0
aiofiles==23.2.1
redis==5.0.3
nats-py==2.7.2
# qdrant-client bumped 1.9.2 -> 1.12.2: 1.9.2 lacked RecommendInput, breaking
# `import isa_common` on a clean install (isA_Cloud#303). 1.10+ adds it.
qdrant-client==1.12.2
aiomqtt==2.0.0
# duckdb bumped 0.10.1 -> 1.4.4 to clear CVE-2024-41672 (PYSEC-2024-203,
# sniff_csv filesystem read), fixed in 1.1.0+ (isA_Cloud#301). Major 0.x->1.x
# jump; client API (connect/execute/fetch*/COPY) unchanged. No persisted
# .duckdb files (default DUCKDB_DATABASE is ':memory:'), so no storage migration.
duckdb==1.4.4

# Offline ed25519 license verification (ADR 0008). Already present transitively
# via the aioboto3 chain; pinned here as a direct dependency.
cryptography==46.0.7

# Transitive pins for the aioboto3 chain (exact-pin strategy).
# urllib3>=2.7.0 clears CVE-2024-37891, -2025-50181, -2025-66418,
# -2025-66471, -2026-21441, -2026-44431.
aiobotocore==2.13.0
botocore==1.34.106
urllib3==2.7.0
