# Qwen3-TTS OpenAI-compatible server — pinned deps.
#
# torch/torchaudio are intentionally NOT pinned here: they are installed in the
# Dockerfile from the cu128 wheel index (sm_120 / Blackwell) BEFORE this file is
# processed, so pip must not pull a default (CPU/cu12x-mismatch) wheel. Keep them
# out of this list.

# --- Qwen3-TTS model runtime ---
qwen-tts==0.3.0
transformers==4.57.1
accelerate==1.10.0
einops==0.8.1
sentencepiece==0.2.1
safetensors==0.5.3

# --- audio I/O + encoding (lossy formats also need the ffmpeg binary, apt-installed) ---
soundfile==0.13.1
librosa==0.11.0
scipy==1.15.2
numpy==2.1.3

# --- HTTP server ---
fastapi==0.118.0
uvicorn[standard]==0.37.0
pydantic==2.11.7
