# Development Environment - Minimal Additional Requirements
#
# NOTE: Core dependencies are installed via: uv pip install -e ".[cloud]"
# This file contains only additional dev-specific packages not in pyproject.toml
#
# Install order:
# 1. uv pip install -e ".[cloud]"  # or other extras like [local], [full-local]
# 2. For centralized logging: uv pip install -e ".[monitoring]"  # includes python-logging-loki
# 3. uv pip install -r deployment/dev/requirements.txt  # additional dev tools

# Development Tools (not in pyproject.toml)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0

# Debugging and Testing
ipython>=8.10.0
ipdb>=0.13.13

# Documentation (if needed)
# mkdocs>=1.5.0
# mkdocs-material>=9.0.0

# Performance Monitoring (optional)
# psutil>=5.9.0

# AI Provider SDKs
cerebras-cloud-sdk>=1.0.0
