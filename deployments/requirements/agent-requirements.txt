# Core Web Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Configuration
python-dotenv>=1.0.0

# AI/LLM Framework (LangGraph + LangChain)
langchain-openai>=0.1.13
langchain-core>=0.2.19
langgraph>=0.1.9
langsmith>=0.1.0

# Durable/Persistence (PostgreSQL Checkpointer)
langgraph-checkpoint-postgres>=1.0.0
langchain-postgres>=0.0.1
supabase>=2.0.0
psycopg[binary,pool]>=3.0.0

# ISA Model - Installed conditionally based on ISA_MODE
# - ISA_MODE=api: Not needed (connects to separate model service)
# - ISA_MODE=local: Installed via start script or Dockerfile with [cloud] or [all] extras
# Note: Do NOT add isa_model here to avoid pulling heavy dependencies

# HTTP Clients
httpx>=0.25.2
aiohttp>=3.8.0

# Serialization (for tracing)
msgpack>=1.0.0

# Multimodal File Upload Support
python-multipart>=0.0.6

# System Monitoring
psutil>=5.9.0

# Service Discovery (Consul)
python-consul>=1.1.0

# Centralized Logging (Loki)
python-logging-loki>=0.3.1 
# Background Job Queue (Phase 2)
celery>=5.3.4
redis>=5.0.1
sse-starlette>=2.0.0
