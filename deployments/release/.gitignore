# Release build artifacts — regenerated by generate-release-manifest.py +
# package-charts.sh on every release cut. Not source; never committed.
releases/*.json
releases/*.offline-bundle.csv
releases/charts/

# Python bytecode
__pycache__/
*.pyc
