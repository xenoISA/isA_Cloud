apiVersion: v1
kind: Service
metadata:
  name: duckdb-grpc
  namespace: isa-cloud-staging
  labels:
    app: duckdb-grpc
    tier: grpc-services
spec:
  type: ClusterIP
  ports:
  - name: grpc
    port: 50052
    targetPort: 50052
    protocol: TCP
  selector:
    app: duckdb-grpc
