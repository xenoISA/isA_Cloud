# Ray Data Cluster — 3-node override
workerGroups:
  - groupName: data-workers
    replicas: 2
    minReplicas: 1
    maxReplicas: 3
    resources:
      requests:
        cpu: "1000m"
        memory: "2Gi"
      limits:
        cpu: "2000m"
        memory: "4Gi"
