FROM grafana/loki:2.9.3

# Copy Loki configuration for staging
COPY configs/staging/loki-config.yml /etc/loki/local-config.yaml

# Expose Loki port
EXPOSE 3100

# Run Loki
CMD ["-config.file=/etc/loki/local-config.yaml"]
