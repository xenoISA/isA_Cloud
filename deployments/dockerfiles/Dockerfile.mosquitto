# Mosquitto MQTT Broker
FROM eclipse-mosquitto:2.0-openssl

# Copy Mosquitto configuration
COPY configs/test/mosquitto.conf /mosquitto/config/mosquitto.conf

# Expose MQTT port
EXPOSE 1883 9001

# Volume for persistence
VOLUME ["/mosquitto/data", "/mosquitto/log"]